{
  "name": "Public Example Flow",
  "timezone": "Pacific/Auckland",
  "browser": {
    "headless": false,
    "channel": "chrome",
    "viewport": {
      "width": 1440,
      "height": 960
    }
  },
  "defaults": {
    "timeoutMs": 15000,
    "navigationTimeoutMs": 30000,
    "fieldDelayMs": 0,
    "navigationDelayMs": 1000
  },
  "notes": [
    "这是公开文档示例，不是生产配置。 / This is a public documentation example, not a production config.",
    "生产选择器和私有自动化逻辑不会放在公开仓库里。 / Production selectors and private automation logic are not published here.",
    "私有授权包会包含真正的运行时。 / Private licensed packages include the actual runtime."
  ]
}
